# QRPipe 实现计划 (Implementation Plan)

> **For agentic workers:** REQUIRED SUB-SKILL: Use ocmar-subagent-driven-development (recommended) or ocmar-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现一个通过二维码动态翻页的单向文件传输套装（PC 纯 HTML 发送端 → 手机 HTTPS 网页连续识别接收端），传输小文本文件，数据全程不经过互联网。

**Architecture:** core/*.mjs 纯 JS/ESM 模块（分页/协议/hash/参数，零依赖，node 与浏览器共用）；sender.html 内联 core 逻辑（file:// 双击即用）；receiver.html 在 HTTPS 下 import core/*.mjs。两端通过统一二维码字符串协议 `<T><P>|<PAYLOAD>` 通信。

**Tech Stack:** 纯 JavaScript (ESM) / Node v20 内置 `node --test` / 浏览器 File API + getUserMedia + IndexedDB / qrcodejs（生成）+ jsQR（解析），库源码置于 lib/。

## Global Constraints（所有 task 隐含遵守，值逐字取自 spec）

- **文件类型**：纯文本（UTF-8）；中文工作环境，代码用英文标识符、UI/注释中文
- **数据隐私（硬约束）**：接收端识别与文件内容全程手机浏览器本地处理，不经过互联网；HTTPS 仅托管静态文件
- **减少依赖（硬约束）**：sender.html 双击 file:// 即用，不依赖 CDN；库源码置于本地 `lib/`；core 零依赖
- **协议版本**：`v=1`；任务序号 `A`–`Z`（恰好 1 字符）；二维码版本上限 `VMAX=10`；最小步长 `MIN_STEP=4`
- **hash**：SHA-256，对文件**原始字节**计算，格式 `sha256:<64位hex>`
- **测试**：Node v20 内置 `node --test`，测试文件置于 `qrpipe/test/*.test.mjs`；脚本用系统 `node` 运行
- **分发结构**：所有产物置于 `qrpipe/` 子目录
- **不 commit**：ocmar 默认不 git add/commit，除非用户显式要求

---

## File Structure

```
qrpipe/
  core/
    paginate.mjs   # 确定性分页（不切断字符）
    protocol.mjs   # 协议编解码 <T><P>|<PAYLOAD>
    hash.mjs       # SHA-256（subtle + 纯JS fallback）
    params.mjs     # QR 容量查表/单步上限/时间预估/版本推荐
    index.mjs      # buildPages/reassemble + 统一导出
  sender.html      # 发送端单文件（内联 core + 引用 lib/qrcode.min.js）
  receiver.html    # 接收端单文件（import core + 引用 lib/jsQR.min.js）
  lib/
    qrcode.min.js  # qrcodejs（生成）
    jsQR.min.js    # jsQR（解析）
  test/
    paginate.test.mjs
    protocol.test.mjs
    hash.test.mjs
    params.test.mjs
    e2e.test.mjs
    sender-html.test.mjs   # 结构检查
    receiver-html.test.mjs # 结构检查
```

---

## Task 1: core/paginate.mjs — 确定性分页（不切断字符）

**Files:**
- Create: `qrpipe/core/paginate.mjs`
- Test: `qrpipe/test/paginate.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces: `MIN_STEP = 4`；`utf8Iterate(bytes: Uint8Array)` → 迭代器，yield `{char: string, byteLen: number}`；`paginate(bytes: Uint8Array, stepBytes: number)` → `string[]`

**Acceptance Criteria:**
- `T1-C1`: 纯 ASCII 按字节切分：`paginate(encode('abcdef'),3)` → `['abc','def']`
- `T1-C2`: 不切断 UTF-8 字符：`paginate(encode('ab中'),4)` → `['ab','中']`（"ab"=2字节后加"中"3字节超4）
- `T1-C3`: 确定性：同输入两次结果 deep equal
- `T1-C4`: 边界——空 → `[]`；4字节 emoji 步长4 → `['😀']`
- `T1-C5`: `stepBytes < 4` 抛 `RangeError`

- [ ] **Step 1: Write the failing test**

```js
// qrpipe/test/paginate.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { paginate, utf8Iterate, MIN_STEP } from '../core/paginate.mjs';

const enc = (s) => new TextEncoder().encode(s);

test('T1-C1 ASCII 按字节切分', () => {
  assert.deepEqual(paginate(enc('abcdef'), 3), ['abc', 'def']);
});
test('T1-C2 不切断 UTF-8 字符', () => {
  assert.deepEqual(paginate(enc('ab中'), 4), ['ab', '中']);
});
test('T1-C3 确定性', () => {
  const b = enc('你好世界hello');
  assert.deepEqual(paginate(b, 7), paginate(b, 7));
});
test('T1-C4 边界: 空 与 emoji', () => {
  assert.deepEqual(paginate(enc(''), 4), []);
  assert.deepEqual(paginate(enc('😀'), 4), ['😀']);
});
test('T1-C5 stepBytes < MIN_STEP 抛 RangeError', () => {
  assert.throws(() => paginate(enc('abc'), 3), RangeError);
});
test('utf8Iterate 产出 char 与 byteLen', () => {
  const it = utf8Iterate(enc('a中'));
  assert.deepEqual(it.next().value, { char: 'a', byteLen: 1 });
  assert.deepEqual(it.next().value, { char: '中', byteLen: 3 });
  assert.equal(it.next().done, true);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test qrpipe/test/paginate.test.mjs`
Expected: FAIL（`Cannot find module .../paginate.mjs`）

- [ ] **Step 3: Write minimal implementation**

```js
// qrpipe/core/paginate.mjs
export const MIN_STEP = 4;

// 按 UTF-8 字节前缀判断单个字符的字节长度
function utf8CharLen(b) {
  if (b < 0x80) return 1;
  if ((b >> 5) === 0b110) return 2;
  if ((b >> 4) === 0b1110) return 3;
  if ((b >> 3) === 0b11110) return 4;
  return 1; // 非法首字节降级为 1（不致死循环）
}

// 逐字符迭代，yield { char, byteLen }；非法 UTF-8 用 U+FFFD 替换字符
export function* utf8Iterate(bytes) {
  const decoder = new TextDecoder('utf-8', { fatal: false });
  let i = 0;
  while (i < bytes.length) {
    const len = Math.min(utf8CharLen(bytes[i]), bytes.length - i);
    const char = decoder.decode(bytes.subarray(i, i + len));
    yield { char, byteLen: len };
    i += len;
  }
}

export function paginate(bytes, stepBytes) {
  if (!(stepBytes >= MIN_STEP)) {
    throw new RangeError(`stepBytes 必须 >= ${MIN_STEP}`);
  }
  const pages = [];
  let cur = '';
  let curBytes = 0;
  for (const { char, byteLen } of utf8Iterate(bytes)) {
    if (curBytes + byteLen > stepBytes && cur !== '') {
      pages.push(cur);
      cur = char;
      curBytes = byteLen;
    } else {
      cur += char;
      curBytes += byteLen;
    }
  }
  if (cur !== '') pages.push(cur);
  return pages;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test qrpipe/test/paginate.test.mjs`
Expected: PASS（6 tests）

- [ ] **Step 5: Record diff (不 commit)**

```bash
git rev-parse HEAD 2>/dev/null || echo "not-a-git-repo"   # baseline
# 任务完成后记录工作树差异作为评审证据：
git diff --stat 2>/dev/null || true
```

---

## Task 2: core/protocol.mjs — 协议编解码

**Files:**
- Create: `qrpipe/core/protocol.mjs`
- Test: `qrpipe/test/protocol.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces: `SEP = '|'`；`isValidTaskId(s)` → bool；`encodePage(taskId, pageNum, payload)` → string（严格校验，非法抛 `TypeError`）；`decodePage(str)` → `{taskId, pageNum, payload}` 或 `null`（宽容，外部输入）；`encodeMeta(meta)` → string(JSON)；`decodeMeta(payload)` → object 或 null

**Acceptance Criteria:**
- `T2-C1`: `encodePage('A',3,'hello')` === `'A3|hello'`
- `T2-C2`: `decodePage('A3|hello')` deepEqual `{taskId:'A',pageNum:3,payload:'hello'}`
- `T2-C3`: 载荷含 `|` 只切第一个：`decodePage('B0|a|b|c').payload` === `'a|b|c'`
- `T2-C4`: 页码变长：`decodePage('A12345|x').pageNum` === `12345`
- `T2-C5`: 非法输入返回 null（`'A3'`、`''`、`'|x'`、`'a3|x'`）
- `T2-C6`: `encodePage` 非法 taskId/pageNum 抛 `TypeError`（`'a'`、`-1`、`1.5`）
- `T2-C7`: meta 编解码对称；第0页元信息 `encodePage('A',0,encodeMeta(meta))` 以 `'A0|'` 开头
- `T2-C8`: 容错前导 UTF-8 BOM（qrcodejs 注入）：`decodePage('\uFEFFA3|hello')` → `{taskId:'A',pageNum:3,payload:'hello'}`

- [ ] **Step 1: Write the failing test**

```js
// qrpipe/test/protocol.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { encodePage, decodePage, encodeMeta, decodeMeta, isValidTaskId, SEP } from '../core/protocol.mjs';

const meta = { v:1, t:'A', name:'x.txt', size:10, pages:2, fps:2, ecc:'M', hash:'sha256:abc' };

test('T2-C1 encodePage', () => { assert.equal(encodePage('A', 3, 'hello'), 'A3|hello'); });
test('T2-C2 decodePage 基本', () => {
  assert.deepEqual(decodePage('A3|hello'), { taskId:'A', pageNum:3, payload:'hello' });
});
test('T2-C3 载荷含 | 切第一个', () => {
  assert.equal(decodePage('B0|a|b|c').payload, 'a|b|c');
});
test('T2-C4 页码变长', () => { assert.equal(decodePage('A12345|x').pageNum, 12345); });
test('T2-C5 非法返回 null', () => {
  assert.equal(decodePage('A3'), null);
  assert.equal(decodePage(''), null);
  assert.equal(decodePage('|x'), null);
  assert.equal(decodePage('a3|x'), null);
});
test('T2-C6 encodePage 严格校验', () => {
  assert.throws(() => encodePage('a', 1, 'x'), TypeError);
  assert.throws(() => encodePage('A', -1, 'x'), TypeError);
  assert.throws(() => encodePage('A', 1.5, 'x'), TypeError);
});
test('T2-C7 meta 对称与第0页', () => {
  assert.deepEqual(decodeMeta(encodeMeta(meta)), meta);
  const p0 = encodePage('A', 0, encodeMeta(meta));
  assert.equal(p0.indexOf('A0|'), 0);
  const d = decodePage(p0);
  assert.equal(d.pageNum, 0);
  assert.deepEqual(decodeMeta(d.payload), meta);
});
test('T2-C8 容错前导 BOM (qrcodejs 注入)', () => {
  assert.deepEqual(decodePage('\uFEFFA3|hello'), { taskId:'A', pageNum:3, payload:'hello' });
});
test('isValidTaskId', () => {
  assert.equal(isValidTaskId('A'), true);
  assert.equal(isValidTaskId('Z'), true);
  assert.equal(isValidTaskId('a'), false);
  assert.equal(isValidTaskId('1'), false);
  assert.equal(isValidTaskId('AB'), false);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test qrpipe/test/protocol.test.mjs`
Expected: FAIL（模块不存在）

- [ ] **Step 3: Write minimal implementation**

```js
// qrpipe/core/protocol.mjs
export const SEP = '|';

export function isValidTaskId(s) {
  return typeof s === 'string' && s.length === 1 && s >= 'A' && s <= 'Z';
}

export function encodePage(taskId, pageNum, payload) {
  if (!isValidTaskId(taskId)) throw new TypeError(`非法 taskId: ${taskId}`);
  if (!Number.isInteger(pageNum) || pageNum < 0) throw new TypeError(`非法 pageNum: ${pageNum}`);
  if (typeof payload !== 'string') throw new TypeError('payload 必须是字符串');
  return `${taskId}${pageNum}${SEP}${payload}`;
}

export function decodePage(str) {
  if (typeof str !== 'string') return null;
  if (str.length > 0 && str.codePointAt(0) === 0xFEFF) str = str.slice(1); // strip 前导 UTF-8 BOM（qrcodejs 对含中文内容注入 EF BB BF）
  if (str.length === 0) return null;
  const i = str.indexOf(SEP);
  if (i < 0) return null;
  const prefix = str.slice(0, i);
  if (prefix.length < 2) return null;            // 至少 taskId + 1 位页码
  const taskId = prefix[0];
  if (!isValidTaskId(taskId)) return null;
  const pageNum = Number(prefix.slice(1));
  if (!Number.isInteger(pageNum) || pageNum < 0) return null;
  return { taskId, pageNum, payload: str.slice(i + 1) };
}

export function encodeMeta(meta) { return JSON.stringify(meta); }
export function decodeMeta(payload) {
  try { const o = JSON.parse(payload); return (o && typeof o === 'object') ? o : null; }
  catch { return null; }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test qrpipe/test/protocol.test.mjs`
Expected: PASS

- [ ] **Step 5: Record diff (不 commit)** — 同 Task 1 Step 5。

---

## Task 3: core/hash.mjs — SHA-256（subtle + 纯 JS fallback）

**Files:**
- Create: `qrpipe/core/hash.mjs`
- Test: `qrpipe/test/hash.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces: `sha256Hex(bytes: Uint8Array)` → `Promise<string>`（async，统一接口）。优先 `crypto.subtle`（secure context），不可用则纯 JS fallback（file:// 必需）

**Acceptance Criteria:**
- `T3-C1`: 空字节 → `'e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855'`（NIST 标准向量）
- `T3-C2`: `'abc'` → `'ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad'`（NIST 标准向量）
- `T3-C3`: 任意中文文本与 `node:crypto` createHash 交叉验证一致
- `T3-C4`: 纯 JS fallback 路径可被强制调用且结果与 subtle 一致（测试通过临时屏蔽 `crypto.subtle` 验证）

- [ ] **Step 1: Write the failing test**

```js
// qrpipe/test/hash.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { createHash } from 'node:crypto';
import { sha256Hex, _pureJsSha256Hex } from '../core/hash.mjs';

const enc = (s) => new TextEncoder().encode(s);

test('T3-C1 空字节 NIST 向量', async () => {
  assert.equal(await sha256Hex(enc('')), 'e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855');
});
test('T3-C2 abc NIST 向量', async () => {
  assert.equal(await sha256Hex(enc('abc')), 'ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad');
});
test('T3-C3 与 node:crypto 交叉验证', async () => {
  const data = enc('你好，世界！QRPipe 测试 hello 1234567890');
  const expected = createHash('sha256').update(data).digest('hex');
  assert.equal(await sha256Hex(data), expected);
});
test('T3-C4 纯 JS fallback 与 subtle 一致', async () => {
  const data = enc('fallback 一致性测试 😀 emoji');
  const expected = createHash('sha256').update(data).digest('hex');
  assert.equal(await _pureJsSha256Hex(data), expected);
  assert.equal(await _pureJsSha256Hex(data), await sha256Hex(data));
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test qrpipe/test/hash.test.mjs`
Expected: FAIL（模块不存在）

- [ ] **Step 3: Write minimal implementation**

```js
// qrpipe/core/hash.mjs
function toHex(bytes) {
  let s = '';
  for (const b of bytes) s += b.toString(16).padStart(2, '0');
  return s;
}

// ---- 纯 JS SHA-256 实现（file:// 无 crypto.subtle 时的 fallback）----
const K = new Uint32Array([
  0x428a2f98,0x71374491,0xb5c0fbcf,0xe9b5dba5,0x3956c25b,0x59f111f1,0x923f82a4,0xab1c5ed5,
  0xd807aa98,0x12835b01,0x243185be,0x550c7dc3,0x72be5d74,0x80deb1fe,0x9bdc06a7,0xc19bf174,
  0xe49b69c1,0xefbe4786,0x0fc19dc6,0x240ca1cc,0x2de92c6f,0x4a7484aa,0x5cb0a9dc,0x76f988da,
  0x983e5152,0xa831c66d,0xb00327c8,0xbf597fc7,0xc6e00bf3,0xd5a79147,0x06ca6351,0x14292967,
  0x27b70a85,0x2e1b2138,0x4d2c6dfc,0x53380d13,0x650a7354,0x766a0abb,0x81c2c92e,0x92722c85,
  0xa2bfe8a1,0xa81a664b,0xc24b8b70,0xc76c51a3,0xd192e819,0xd6990624,0xf40e3585,0x106aa070,
  0x19a4c116,0x1e376c08,0x2748774c,0x34b0bcb5,0x391c0cb3,0x4ed8aa4a,0x5b9cca4f,0x682e6ff3,
  0x748f82ee,0x78a5636f,0x84c87814,0x8cc70208,0x90befffa,0xa4506ceb,0xbef9a3f7,0xc67178f2,
]);

function rotr(x, n) { return ((x >>> n) | (x << (32 - n))) >>> 0; }

export function _pureJsSha256Hex(data) {
  // 填充
  const bitLen = data.length * 8;
  const withPad = data.length + 1 + ((56 - (data.length + 1) % 64 + 64) % 64) + 8;
  const m = new Uint8Array(withPad);
  m.set(data);
  m[data.length] = 0x80;
  // 64 位大端长度（仅低 32 位足够，文件 < 512MB）
  const dv = new DataView(m.buffer);
  dv.setUint32(withPad - 4, bitLen >>> 0, false);
  dv.setUint32(withPad - 8, Math.floor(bitLen / 0x100000000), false);

  let h0=0x6a09e667,h1=0xbb67ae85,h2=0x3c6ef372,h3=0xa54ff53a;
  let h4=0x510e527f,h5=0x9b05688c,h6=0x1f83d9ab,h7=0x5be0cd19;
  const w = new Uint32Array(64);

  for (let off = 0; off < m.length; off += 64) {
    for (let i = 0; i < 16; i++) w[i] = dv.getUint32(off + i * 4, false);
    for (let i = 16; i < 64; i++) {
      const s0 = rotr(w[i-15],7) ^ rotr(w[i-15],18) ^ (w[i-15] >>> 3);
      const s1 = rotr(w[i-2],17) ^ rotr(w[i-2],19) ^ (w[i-2] >>> 10);
      w[i] = (w[i-16] + s0 + w[i-7] + s1) >>> 0;
    }
    let a=h0,b=h1,c=h2,d=h3,e=h4,f=h5,g=h6,hh=h7;
    for (let i = 0; i < 64; i++) {
      const S1 = rotr(e,6) ^ rotr(e,11) ^ rotr(e,25);
      const ch = (e & f) ^ (~e & g);
      const t1 = (hh + S1 + ch + K[i] + w[i]) >>> 0;
      const S0 = rotr(a,2) ^ rotr(a,13) ^ rotr(a,22);
      const maj = (a & b) ^ (a & c) ^ (b & c);
      const t2 = (S0 + maj) >>> 0;
      hh=g; g=f; f=e; e=(d + t1) >>> 0; d=c; c=b; b=a; a=(t1 + t2) >>> 0;
    }
    h0=(h0+a)>>>0; h1=(h1+b)>>>0; h2=(h2+c)>>>0; h3=(h3+d)>>>0;
    h4=(h4+e)>>>0; h5=(h5+f)>>>0; h6=(h6+g)>>>0; h7=(h7+hh)>>>0;
  }
  const out = new Uint8Array(32);
  const ov = new DataView(out.buffer);
  [h0,h1,h2,h3,h4,h5,h6,h7].forEach((v,i)=>ov.setUint32(i*4, v, false));
  return toHex(out);
}

export async function sha256Hex(bytes) {
  const subtle = globalThis.crypto && globalThis.crypto.subtle;
  if (subtle) {
    try {
      const d = await subtle.digest('SHA-256', bytes);
      return toHex(new Uint8Array(d));
    } catch { /* 落入 fallback */ }
  }
  return _pureJsSha256Hex(bytes);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test qrpipe/test/hash.test.mjs`
Expected: PASS

- [ ] **Step 5: Record diff (不 commit)** — 同 Task 1 Step 5。

---

## Task 4: core/params.mjs — QR 容量/单步上限/时间/版本

**Files:**
- Create: `qrpipe/core/params.mjs`
- Test: `qrpipe/test/params.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces: `VMAX=10`；`ECC_LEVELS=['L','M','Q','H']`；`QR_CAPACITY`（V1–10 字节模式容量表）；`maxStepBytes(versionMax, ecc, pageDigits)` → number；`estimateTime(pages, fps)` → number|null；`recommendVersion(stepBytes, ecc, pageDigits)` → number|null

**Acceptance Criteria:**
- `T4-C1`: `QR_CAPACITY[10].M === 213` 且 `QR_CAPACITY[1].L === 17`
- `T4-C2`: `maxStepBytes(10,'M',2)` === `209`（213 − (1+2+1)）
- `T4-C3`: `estimateTime(10,2)` === `5`；`estimateTime(10,0)` === `null`
- `T4-C4`: `recommendVersion(150,'M',2)` === `9`（V9 M=180−4=176≥150，V8=148<150）
- `T4-C5`: `recommendVersion(300,'M',2)` === `null`（超 VMAX）
- `T4-C6`: 容量随版本单调递增（V[n].L ≤ V[n+1].L 对所有 n）

- [ ] **Step 1: Write the failing test**

```js
// qrpipe/test/params.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { QR_CAPACITY, VMAX, ECC_LEVELS, maxStepBytes, estimateTime, recommendVersion } from '../core/params.mjs';

test('T4-C1 容量锚点值', () => {
  assert.equal(QR_CAPACITY[10].M, 213);
  assert.equal(QR_CAPACITY[1].L, 17);
});
test('T4-C2 maxStepBytes', () => {
  assert.equal(maxStepBytes(10, 'M', 2), 209);
});
test('T4-C3 estimateTime', () => {
  assert.equal(estimateTime(10, 2), 5);
  assert.equal(estimateTime(10, 0), null);
});
test('T4-C4 recommendVersion 正常', () => {
  assert.equal(recommendVersion(150, 'M', 2), 9);
});
test('T4-C5 recommendVersion 超限', () => {
  assert.equal(recommendVersion(300, 'M', 2), null);
});
test('T4-C6 容量单调递增', () => {
  for (let v = 1; v < VMAX; v++) {
    for (const e of ECC_LEVELS) assert.ok(QR_CAPACITY[v][e] <= QR_CAPACITY[v+1][e], `V${v}.${e}`);
  }
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test qrpipe/test/params.test.mjs`
Expected: FAIL（模块不存在）

- [ ] **Step 3: Write minimal implementation**

```js
// qrpipe/core/params.mjs
export const VMAX = 10;
export const ECC_LEVELS = ['L', 'M', 'Q', 'H'];

// QR 字节模式（8-bit byte）容量表，Version 1..10（ISO/IEC 18004 / thonky 参考）
export const QR_CAPACITY = {
  1:  { L:17,  M:14,  Q:11,  H:7   },
  2:  { L:32,  M:26,  Q:20,  H:14  },
  3:  { L:53,  M:42,  Q:32,  H:24  },
  4:  { L:78,  M:62,  Q:46,  H:34  },
  5:  { L:106, M:84,  Q:66,  H:44  },
  6:  { L:134, M:106, Q:86,  H:60  },
  7:  { L:154, M:122, Q:108, H:74  },
  8:  { L:192, M:152, Q:130, H:86  },
  9:  { L:230, M:180, Q:150, H:98  },
  10: { L:271, M:213, Q:151, H:119 },
};

// 单步内容字节上限 = 容量 − 协议开销(taskId 1 + 页码位数 + 分隔符 1)
export function maxStepBytes(versionMax, ecc, pageDigits) {
  const cap = QR_CAPACITY[versionMax]?.[ecc];
  if (cap == null) throw new RangeError(`非法 version/ecc: ${versionMax}/${ecc}`);
  return cap - (1 + pageDigits + 1);
}

// 总传输时间（秒）；fps=0 手动模式返回 null
export function estimateTime(pages, fps) {
  if (!fps) return null;
  return pages / fps;
}

// 推荐所需最小版本；超 VMAX 返回 null
export function recommendVersion(stepBytes, ecc, pageDigits) {
  for (let v = 1; v <= VMAX; v++) {
    if (maxStepBytes(v, ecc, pageDigits) >= stepBytes) return v;
  }
  return null;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test qrpipe/test/params.test.mjs`
Expected: PASS

- [ ] **Step 5: Record diff (不 commit)** — 同 Task 1 Step 5。

---

## Task 5: core/index.mjs + 端到端 — buildPages / reassemble

**Files:**
- Create: `qrpipe/core/index.mjs`
- Test: `qrpipe/test/e2e.test.mjs`

**Interfaces:**
- Consumes: Task 1 (`paginate`), Task 2 (`encodePage/encodeMeta/decodePage/decodeMeta/isValidTaskId`), Task 3 (`sha256Hex`), Task 4 (`recommendVersion/maxStepBytes/VMAX`)
- Produces:
  - `buildPages(bytes, stepBytes, taskId, {name, fps, ecc})` → `Promise<{meta, metaPage, dataPages: string[], pages, hash}>`
  - `reassemble(meta, collected: Map<number,string>)` → `Promise<{ok, bytes?, missing: number[], hashOk?}>`
  - 统一 re-export core 全部公开 API

**Acceptance Criteria:**
- `T5-C1` (SC3 端到端还原): 中文文本经 `buildPages` → 逐页 `decodePage` 收集进 Map → `reassemble` → `ok=true`、`hashOk=true`、`decode(bytes)===原文`
- `T5-C2` (SC4 可复现): 同输入两次 `buildPages`，`metaPage+dataPages` 逐字节相同
- `T5-C3` (SC5 缺页): 丢弃第 3 页 → `reassemble` `ok=false`、`missing` 含 3
- `T5-C4` (补传): 缺第 3 页时 `missing=[3]`；补回后 `ok=true`
- `T5-C5`: 默认参数（stepBytes=150, ecc='M'）下，示例小文件 `recommendVersion` 非 null（参数合法）

- [ ] **Step 1: Write the failing test**

```js
// qrpipe/test/e2e.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildPages, reassemble, decodePage, decodeMeta, recommendVersion } from '../core/index.mjs';

const enc = (s) => new TextEncoder().encode(s);
const dec = (b) => new TextDecoder().decode(b);

const TEXT = '你好，QRPipe！这是一段用于端到端测试的中文与 English 混合文本。\n第二行换行。12345';

async function collectAll(built) {
  const map = new Map();
  for (const s of built.dataPages) {
    const d = decodePage(s);
    map.set(d.pageNum, d.payload);
  }
  return map;
}

test('T5-C1 端到端还原', async () => {
  const built = await buildPages(enc(TEXT), 30, 'A', { name:'demo.txt', fps:2, ecc:'M' });
  const map = await collectAll(built);
  const res = await reassemble(built.meta, map);
  assert.equal(res.ok, true);
  assert.equal(res.hashOk, true);
  assert.equal(dec(res.bytes), TEXT);
});
test('T5-C2 可复现', async () => {
  const a = await buildPages(enc(TEXT), 30, 'A', { name:'demo.txt', fps:2, ecc:'M' });
  const b = await buildPages(enc(TEXT), 30, 'A', { name:'demo.txt', fps:2, ecc:'M' });
  assert.deepEqual([a.metaPage, ...a.dataPages], [b.metaPage, ...b.dataPages]);
});
test('T5-C3 缺页', async () => {
  const built = await buildPages(enc(TEXT), 30, 'A', { name:'demo.txt', fps:2, ecc:'M' });
  const map = await collectAll(built);
  map.delete(3);
  const res = await reassemble(built.meta, map);
  assert.equal(res.ok, false);
  assert.ok(res.missing.includes(3));
});
test('T5-C4 补传后齐全', async () => {
  const built = await buildPages(enc(TEXT), 30, 'A', { name:'demo.txt', fps:2, ecc:'M' });
  const map = await collectAll(built);
  map.delete(3);
  const r1 = await reassemble(built.meta, map);
  assert.deepEqual(r1.missing, [3]);
  // 模拟补传：从原序列找回第3页
  const again = await collectAll(built);
  map.set(3, again.get(3));
  const r2 = await reassemble(built.meta, map);
  assert.equal(r2.ok, true);
});
test('T5-C5 默认参数合法', async () => {
  const built = await buildPages(enc(TEXT), 150, 'A', { name:'demo.txt', fps:2, ecc:'M' });
  const digits = String(built.pages).length;
  assert.ok(recommendVersion(150, 'M', digits) != null);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test qrpipe/test/e2e.test.mjs`
Expected: FAIL（模块不存在）

- [ ] **Step 3: Write minimal implementation**

```js
// qrpipe/core/index.mjs
import { paginate } from './paginate.mjs';
import { encodePage, encodeMeta, decodePage, decodeMeta, isValidTaskId, SEP } from './protocol.mjs';
import { sha256Hex } from './hash.mjs';
import { recommendVersion, maxStepBytes, VMAX, ECC_LEVELS, QR_CAPACITY, estimateTime } from './params.mjs';

export { paginate, encodePage, encodeMeta, decodePage, decodeMeta, isValidTaskId, SEP };
export { sha256Hex, _pureJsSha256Hex } from './hash.mjs';
export { recommendVersion, maxStepBytes, VMAX, ECC_LEVELS, QR_CAPACITY, estimateTime };
export { utf8Iterate, MIN_STEP } from './paginate.mjs';

// 构造发送序列：第0页元信息 + 数据页
export async function buildPages(bytes, stepBytes, taskId, { name, fps, ecc }) {
  if (!isValidTaskId(taskId)) throw new TypeError(`非法 taskId: ${taskId}`);
  const hash = 'sha256:' + await sha256Hex(bytes);
  const chunks = paginate(bytes, stepBytes);
  const pages = chunks.length;
  // 空文件 → pages=0；规范要求 pages>=1（至少 1 页）。空文件应由上层拦截，这里保证 pages>=1
  const meta = {
    v: 1, t: taskId, name, size: bytes.length,
    pages: Math.max(1, pages), fps, ecc, hash,
  };
  const metaPage = encodePage(taskId, 0, encodeMeta(meta));
  const dataPages = chunks.map((c, i) => encodePage(taskId, i + 1, c));
  return { meta, metaPage, dataPages, pages: meta.pages, hash };
}

// 接收端拼装：检查缺页 → 顺序拼接 → hash 校验
export async function reassemble(meta, collected) {
  const missing = [];
  for (let p = 1; p <= meta.pages; p++) {
    if (!collected.has(p)) missing.push(p);
  }
  if (missing.length) return { ok: false, missing };
  let text = '';
  for (let p = 1; p <= meta.pages; p++) text += collected.get(p);
  const bytes = new TextEncoder().encode(text);
  const hash = 'sha256:' + await sha256Hex(bytes);
  return { ok: true, bytes, missing: [], hashOk: hash === meta.hash };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test qrpipe/test/e2e.test.mjs`
Expected: PASS

- [ ] **Step 5: Record diff (不 commit)** — 同 Task 1 Step 5。

---

## Task 6: sender.html — 发送端单文件（内联 core + qrcode）

**Files:**
- Create: `qrpipe/lib/qrcode.min.js`（下载 qrcodejs）
- Create: `qrpipe/sender.html`（内联 core 逻辑 + UI + 翻页 + 重传）
- Test: `qrpipe/test/sender-html.test.mjs`（结构检查）
- 手动验证清单（见 T6-C4/C5）

**Interfaces:**
- Consumes: Task 1–5 的 core 逻辑（**内联**进 `<script type="module">`，因 file:// 下 ESM import 受限）；`lib/qrcode.min.js`（经典 `<script>`，挂全局 `QRCode`）
- Produces: 可双击运行的 `sender.html`；用户交互生成并翻页展示二维码

**Acceptance Criteria:**
- `T6-C1`: `qrpipe/lib/qrcode.min.js` 存在且非空（>1KB）
- `T6-C2`: `sender.html` 含：`<input type="file">`、步长/fps/ecc/任务序号 控件、二维码展示容器、参数概要区、翻页控件（自动/手动）、重传输入
- `T6-C3`: `sender.html` 内联 core 关键函数（`paginate`、`encodePage`、`encodeMeta`、`sha256Hex`、`buildPages`、`recommendVersion`）——结构测试 grep 命中
- `T6-C4`（手动）: 双击 `sender.html` 在 Chrome/Edge 打开 → 选示例 txt → 显示参数概要（大小/页数/单步字节/QR版本/总时间/hash）→ 确认 → 展示第0页二维码 → 自动翻页至末页
- `T6-C5`（手动）: 重传模式——输入页码 → 展示对应页二维码（内容与首次一致）

- [ ] **Step 1: 获取 qrcodejs 库到 lib/**

```bash
mkdir -p qrpipe/lib
curl -fsSL https://cdn.jsdelivr.net/npm/qrcodejs@1.0.0/qrcode.min.js -o qrpipe/lib/qrcode.min.js
test -s qrpipe/lib/qrcode.min.js && echo "OK qrcode downloaded" || echo "FAIL"
```
Expected: 输出 `OK qrcode downloaded`

- [ ] **Step 2: Write the structure-check test**

```js
// qrpipe/test/sender-html.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync, statSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

const here = dirname(fileURLToPath(import.meta.url));
const read = (p) => readFileSync(join(here, '..', p), 'utf8');

test('T6-C1 lib/qrcode.min.js 非空', () => {
  const st = statSync(join(here, '..', 'lib', 'qrcode.min.js'));
  assert.ok(st.size > 1024, 'qrcode.min.js 应大于 1KB');
});
test('T6-C2 sender.html 关键 UI 元素', () => {
  const html = read('sender.html');
  assert.ok(html.includes('type="file"'));
  assert.ok(/步长|step/i.test(html));
  assert.ok(/任务序号|task/i.test(html));
  assert.ok(html.includes('QRCode'));
  assert.ok(/重传|retransmit|retrans/i.test(html));
});
test('T6-C3 sender.html 内联 core 关键函数', () => {
  const html = read('sender.html');
  for (const fn of ['paginate', 'encodePage', 'encodeMeta', 'sha256Hex', 'buildPages', 'recommendVersion']) {
    assert.ok(html.includes(fn), `sender.html 应内联 ${fn}`);
  }
});
```

- [ ] **Step 3: Run test to verify it fails**

Run: `node --test qrpipe/test/sender-html.test.mjs`
Expected: FAIL（sender.html 不存在）

- [ ] **Step 4: Write sender.html (内联 core + UI)**

> **DRY 说明**：file:// 双击打开时浏览器拒绝 ES module 的跨文件 `import`（CORS），因此 sender.html 必须**内联** core 逻辑。下方 `<script type="module">` 内联的函数体应与 `core/*.mjs` **逐字一致**（保留 `export` 无害）。修改 core 后须同步此处。

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<title>QRPipe 发送端</title>
<!-- qrcodejs：经典 script，file:// 下可加载同目录 js -->
<script src="lib/qrcode.min.js"></script>
<style>
  body { font-family: system-ui, sans-serif; max-width: 720px; margin: 24px auto; padding: 0 16px; }
  .row { margin: 8px 0; }
  label { display: inline-block; width: 90px; }
  #qrcode { margin: 16px 0; }
  #qrcode img, #qrcode canvas { display: block; margin: auto; }
  #summary { background: #f5f5f5; padding: 12px; border-radius: 6px; white-space: pre-wrap; }
  button { padding: 6px 14px; margin-right: 8px; }
  .hidden { display: none; }
</style>
</head>
<body>
<h1>QRPipe 发送端</h1>

<div id="setup">
  <div class="row"><label>文件：</label><input type="file" id="file"></div>
  <div class="row"><label>任务序号：</label><input type="text" id="taskId" maxlength="1" value="A" style="width:40px;text-transform:uppercase"></div>
  <div class="row"><label>步长(字节)：</label><input type="number" id="step" value="150" min="4"></div>
  <div class="row"><label>频率(张/秒)：</label><input type="number" id="fps" value="2" min="0" step="0.5">（0=手动）</div>
  <div class="row"><label>容错率：</label>
    <select id="ecc"><option>L</option><option selected>M</option><option>Q</option><option>H</option></select>
  </div>
  <div class="row"><button id="analyze">分析文件</button></div>
</div>

<div id="summaryBox" class="hidden">
  <h3>传输概要</h3>
  <div id="summary"></div>
  <button id="confirm">确认开始</button>
  <button id="back">返回修改</button>
</div>

<div id="transmit" class="hidden">
  <div id="qrcode"></div>
  <div class="row" id="pageInfo"></div>
  <button id="prev">◀ 上一页</button>
  <button id="next">▶ 下一页</button>
  <button id="pause">⏸ 暂停/继续</button>
  <button id="stop">⏹ 停止</button>
  <hr>
  <div class="row">重传：输入页码（逗号分隔，如 3,5,7）<input type="text" id="retrans" style="width:160px"><button id="retransBtn">跳转重传</button></div>
</div>

<script type="module">
// ===== 内联 core（与 core/*.mjs 逐字一致，含 export 无害）=====
const MIN_STEP = 4;
function utf8CharLen(b){ if(b<0x80)return 1; if((b>>5)===0b110)return 2; if((b>>4)===0b1110)return 3; if((b>>3)===0b11110)return 4; return 1; }
function* utf8Iterate(bytes){ const dec=new TextDecoder('utf-8',{fatal:false}); let i=0; while(i<bytes.length){ const len=Math.min(utf8CharLen(bytes[i]),bytes.length-i); yield {char:dec.decode(bytes.subarray(i,i+len)),byteLen:len}; i+=len; } }
export function paginate(bytes, stepBytes){ if(!(stepBytes>=MIN_STEP)) throw new RangeError(`stepBytes 必须 >= ${MIN_STEP}`); const pages=[]; let cur='',cb=0; for(const {char,byteLen} of utf8Iterate(bytes)){ if(cb+byteLen>stepBytes && cur!==''){ pages.push(cur); cur=char; cb=byteLen; } else { cur+=char; cb+=byteLen; } } if(cur!=='') pages.push(cur); return pages; }

const SEP='|';
function isValidTaskId(s){ return typeof s==='string' && s.length===1 && s>='A' && s<='Z'; }
export function encodePage(taskId, pageNum, payload){ if(!isValidTaskId(taskId)) throw new TypeError('非法 taskId'); if(!Number.isInteger(pageNum)||pageNum<0) throw new TypeError('非法 pageNum'); return `${taskId}${pageNum}${SEP}${payload}`; }
export function decodePage(str){ if(typeof str!=='string') return null; if(str.length>0&&str.codePointAt(0)===0xFEFF) str=str.slice(1); if(str.length===0) return null; const i=str.indexOf(SEP); if(i<0) return null; const pre=str.slice(0,i); if(pre.length<2) return null; const t=pre[0]; if(!isValidTaskId(t)) return null; const p=Number(pre.slice(1)); if(!Number.isInteger(p)||p<0) return null; return {taskId:t,pageNum:p,payload:str.slice(i+1)}; }
export function encodeMeta(m){ return JSON.stringify(m); }
export function decodeMeta(p){ try{ const o=JSON.parse(p); return (o&&typeof o==='object')?o:null; }catch{ return null; } }

// ---- hash: subtle + 纯JS fallback ----
function toHex(bytes){ let s=''; for(const b of bytes) s+=b.toString(16).padStart(2,'0'); return s; }
const K=new Uint32Array([0x428a2f98,0x71374491,0xb5c0fbcf,0xe9b5dba5,0x3956c25b,0x59f111f1,0x923f82a4,0xab1c5ed5,0xd807aa98,0x12835b01,0x243185be,0x550c7dc3,0x72be5d74,0x80deb1fe,0x9bdc06a7,0xc19bf174,0xe49b69c1,0xefbe4786,0x0fc19dc6,0x240ca1cc,0x2de92c6f,0x4a7484aa,0x5cb0a9dc,0x76f988da,0x983e5152,0xa831c66d,0xb00327c8,0xbf597fc7,0xc6e00bf3,0xd5a79147,0x06ca6351,0x14292967,0x27b70a85,0x2e1b2138,0x4d2c6dfc,0x53380d13,0x650a7354,0x766a0abb,0x81c2c92e,0x92722c85,0xa2bfe8a1,0xa81a664b,0xc24b8b70,0xc76c51a3,0xd192e819,0xd6990624,0xf40e3585,0x106aa070,0x19a4c116,0x1e376c08,0x2748774c,0x34b0bcb5,0x391c0cb3,0x4ed8aa4a,0x5b9cca4f,0x682e6ff3,0x748f82ee,0x78a5636f,0x84c87814,0x8cc70208,0x90befffa,0xa4506ceb,0xbef9a3f7,0xc67178f2]);
function rotr(x,n){ return ((x>>>n)|(x<<(32-n)))>>>0; }
function _pureJsSha256Hex(data){ const bitLen=data.length*8; const len=data.length+1+((56-(data.length+1)%64+64)%64)+8; const m=new Uint8Array(len); m.set(data); m[data.length]=0x80; const dv=new DataView(m.buffer); dv.setUint32(len-4,bitLen>>>0,false); dv.setUint32(len-8,Math.floor(bitLen/0x100000000),false); let h0=0x6a09e667,h1=0xbb67ae85,h2=0x3c6ef372,h3=0xa54ff53a,h4=0x510e527f,h5=0x9b05688c,h6=0x1f83d9ab,h7=0x5be0cd19; const w=new Uint32Array(64); for(let off=0;off<m.length;off+=64){ for(let i=0;i<16;i++) w[i]=dv.getUint32(off+i*4,false); for(let i=16;i<64;i++){ const s0=rotr(w[i-15],7)^rotr(w[i-15],18)^(w[i-15]>>>3); const s1=rotr(w[i-2],17)^rotr(w[i-2],19)^(w[i-2]>>>10); w[i]=(w[i-16]+s0+w[i-7]+s1)>>>0; } let a=h0,b=h1,c=h2,d=h3,e=h4,f=h5,g=h6,hh=h7; for(let i=0;i<64;i++){ const S1=rotr(e,6)^rotr(e,11)^rotr(e,25); const ch=(e&f)^(~e&g); const t1=(hh+S1+ch+K[i]+w[i])>>>0; const S0=rotr(a,2)^rotr(a,13)^rotr(a,22); const maj=(a&b)^(a&c)^(b&c); const t2=(S0+maj)>>>0; hh=g;g=f;f=e;e=(d+t1)>>>0;d=c;c=b;b=a;a=(t1+t2)>>>0; } h0=(h0+a)>>>0;h1=(h1+b)>>>0;h2=(h2+c)>>>0;h3=(h3+d)>>>0;h4=(h4+e)>>>0;h5=(h5+f)>>>0;h6=(h6+g)>>>0;h7=(h7+hh)>>>0; } const out=new Uint8Array(32); const ov=new DataView(out.buffer); [h0,h1,h2,h3,h4,h5,h6,h7].forEach((v,i)=>ov.setUint32(i*4,v,false)); return toHex(out); }
export async function sha256Hex(bytes){ const sub=globalThis.crypto&&globalThis.crypto.subtle; if(sub){ try{ const d=await sub.digest('SHA-256',bytes); return toHex(new Uint8Array(d)); }catch{} } return _pureJsSha256Hex(bytes); }

// ---- params ----
const VMAX=10; const ECC_LEVELS=['L','M','Q','H'];
const QR_CAPACITY={1:{L:17,M:14,Q:11,H:7},2:{L:32,M:26,Q:20,H:14},3:{L:53,M:42,Q:32,H:24},4:{L:78,M:62,Q:46,H:34},5:{L:106,M:84,Q:66,H:44},6:{L:134,M:106,Q:86,H:60},7:{L:154,M:122,Q:108,H:74},8:{L:192,M:152,Q:130,H:86},9:{L:230,M:180,Q:150,H:98},10:{L:271,M:213,Q:151,H:119}};
function maxStepBytes(vMax,ecc,digits){ return QR_CAPACITY[vMax][ecc]-(1+digits+1); }
export function recommendVersion(stepBytes,ecc,digits){ for(let v=1;v<=VMAX;v++){ if(maxStepBytes(v,ecc,digits)>=stepBytes) return v; } return null; }
function estimateTime(pages,fps){ return fps? pages/fps : null; }

// ---- buildPages ----
export async function buildPages(bytes, stepBytes, taskId, {name, fps, ecc}){
  const hash='sha256:'+await sha256Hex(bytes);
  const chunks=paginate(bytes, stepBytes);
  const pages=Math.max(1, chunks.length);
  const meta={v:1,t:taskId,name,size:bytes.length,pages,fps,ecc,hash};
  const metaPage=encodePage(taskId,0,encodeMeta(meta));
  const dataPages=chunks.map((c,i)=>encodePage(taskId,i+1,c));
  return {meta,metaPage,dataPages,pages,hash};
}

// ===== UI 逻辑 =====
const $ = (id) => document.getElementById(id);
let built = null;        // {meta, metaPage, dataPages, pages}
let playList = null;     // 当前播放的字符串数组（含第0页）
let idx = 0;
let timer = null;
let fps = 2;

function renderQR(str){
  const box = $('qrcode'); box.innerHTML='';
  // ECC 级别映射到 qrcodejs 级别字符串
  const level = $('ecc').value;
  new QRCode(box, { text: str, width: 320, height: 320, correctLevel: QRCode.CorrectLevel[level] });
}

function showPage(i){
  idx = Math.max(0, Math.min(i, playList.length-1));
  renderQR(playList[idx]);
  $('pageInfo').textContent = idx===0 ? '【第 0 页 · 任务元信息】' : `第 ${idx} / ${playList.length-1} 页`;
}

function startAuto(){
  stopAuto();
  if(!fps) return; // 手动
  timer = setInterval(()=>{ if(idx < playList.length-1) showPage(idx+1); else stopAuto(); }, 1000/fps);
}
function stopAuto(){ if(timer){ clearInterval(timer); timer=null; } }

$('analyze').addEventListener('click', async ()=>{
  const f = $('file').files[0];
  if(!f){ alert('请先选择文件'); return; }
  const taskId = $('taskId').value.toUpperCase();
  if(!/^[A-Z]$/.test(taskId)){ alert('任务序号必须是 1 位大写字母'); return; }
  const step = Number($('step').value);
  if(!(step>=4)){ alert('步长必须 >= 4'); return; }
  fps = Number($('fps').value);
  const ecc = $('ecc').value;
  const buf = new Uint8Array(await f.arrayBuffer());
  if(buf.length===0){ alert('文件为空'); return; }
  built = await buildPages(buf, step, taskId, {name:f.name, fps, ecc});
  // 参数校验：所需版本
  const digits = String(built.pages).length; // 数据页页码最大位数
  const ver = recommendVersion(step + 3, ecc, digits); // +3 预留 qrcodejs 注入的 UTF-8 BOM
  const tooLarge = ver == null;
  const t = estimateTime(built.pages, fps);
  $('summary').textContent =
    `文件名：${f.name}\n`+
    `大小：${buf.length} 字节\n`+
    `任务序号：${taskId}\n`+
    `总数据页：${built.pages} 页（+ 第0页元信息）\n`+
    `步长：${step} 字节\n`+
    `容错率：${ecc}\n`+
    `翻页频率：${fps? fps+' 张/秒':'手动'}\n`+
    `所需 QR 版本：${ver!=null? ver : '超出 V'+VMAX+' 上限'}${tooLarge? '（请减小步长或容错率）':''}\n`+
    `预计总时间：${t!=null? (t>=60? (t/60).toFixed(1)+' 分钟' : t.toFixed(1)+' 秒'):'手动模式'}\n`+
    `hash：${built.hash}`;
  $('confirm').disabled = tooLarge; // 步长超限禁止开始，避免生成过大二维码
  $('setup').classList.add('hidden');
  $('summaryBox').classList.remove('hidden');
});

$('back').addEventListener('click', ()=>{
  $('summaryBox').classList.add('hidden');
  $('setup').classList.remove('hidden');
});

$('confirm').addEventListener('click', ()=>{
  $('summaryBox').classList.add('hidden');
  $('transmit').classList.remove('hidden');
  playList = [built.metaPage, ...built.dataPages];
  showPage(0);
  // 第0页停留 2 秒后自动开始（若非手动）
  if(fps){ setTimeout(()=>startAuto(), 2000); }
});

$('prev').addEventListener('click', ()=>{ showPage(idx-1); });
$('next').addEventListener('click', ()=>{ showPage(idx+1); });
$('pause').addEventListener('click', ()=>{ if(timer){ stopAuto(); } else { startAuto(); } });
$('stop').addEventListener('click', ()=>{ stopAuto(); alert('传输已停止。可在下方输入页码重传。'); });

$('retransBtn').addEventListener('click', ()=>{
  const raw = $('retrans').value.trim();
  if(!raw) return;
  const nums = raw.split(',').map(s=>Number(s.trim())).filter(n=>Number.isInteger(n) && n>=1 && n<=built.pages);
  if(nums.length===0){ alert('请输入有效页码'); return; }
  // 进入补传：只播放指定页（与首次内容一致，确定性）
  stopAuto();
  playList = nums.map(n=>built.dataPages[n-1]);
  showPage(0);
});
</script>
</body>
</html>
```

- [ ] **Step 5: Run test to verify it passes**

Run: `node --test qrpipe/test/sender-html.test.mjs`
Expected: PASS（3 tests）

- [ ] **Step 6: 手动浏览器验证 (T6-C4/C5)**

在浏览器（Chrome/Edge）中双击打开 `qrpipe/sender.html`：
1. 选一个示例 txt（含中英文），点"分析文件" → 查看概要（页数/版本/时间/hash 合理）
2. 点"确认开始" → 第0页二维码显示 → 自动翻页至末页
3. 停止后输入页码（如 `3,5`）→ "跳转重传" → 展示对应页二维码

Expected: 全流程正常，二维码清晰可扫。

- [ ] **Step 7: Record diff (不 commit)** — 同 Task 1 Step 5。

---

## Task 7: receiver.html — 接收端（HTTPS 托管，连续识别）

**Files:**
- Create: `qrpipe/lib/jsQR.min.js`（下载 jsQR）
- Create: `qrpipe/receiver.html`（import core/*.mjs + getUserMedia + jsQR + IndexedDB + 拼装校验）
- Test: `qrpipe/test/receiver-html.test.mjs`（结构检查）
- 手动验证清单（见 T7-C5）

**Interfaces:**
- Consumes: Task 1–5 的 core 逻辑（HTTPS 下用 `import` 引用 `./core/*.mjs`）；`lib/jsQR.min.js`（经典 `<script>`，挂全局 `jsQR`）
- Produces: 可在手机浏览器（HTTPS）打开的 `receiver.html`，连续识别、本地收集、拼装校验、下载

**Acceptance Criteria:**
- `T7-C1`: `qrpipe/lib/jsQR.min.js` 存在且非空（>5KB）
- `T7-C2`: `receiver.html` import core（`import ... from './core/index.mjs'`）+ 引用 `lib/jsQR.min.js` + 含 `getUserMedia`、`requestAnimationFrame`/截帧、IndexedDB 调用
- `T7-C3`: `receiver.html` 含进度/缺页 UI、拼装（`reassemble`）、hash 校验、下载按钮
- `T7-C4`: 接收端逻辑校验——用 node 跑一个脚本：构造 `built`（core），模拟"扫描得到二维码字符串"喂给 receiver 的解析+收集+reassemble 流程（提取 receiver 的纯函数或复用 core），结果 `ok=true`。简化为：复用 core `decodePage`+`reassemble`，验证 T7 解析路径与 core 一致（结构测试 + core 复用）
- `T7-C5`（手动）: 手机浏览器（HTTPS 托管）打开 receiver → 授权摄像头 → 扫描 sender 的第0页→数据页 → 进度更新 → 齐全后自动校验 hash → 通过则下载还原文件

- [ ] **Step 1: 获取 jsQR 库到 lib/**

```bash
curl -fsSL https://cdn.jsdelivr.net/npm/jsqr@1.4.0/dist/jsQR.js -o qrpipe/lib/jsQR.min.js
test -s qrpipe/lib/jsQR.min.js && echo "OK jsQR downloaded" || echo "FAIL"
```
Expected: `OK jsQR downloaded`

- [ ] **Step 2: Write the structure-check test**

```js
// qrpipe/test/receiver-html.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync, statSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

const here = dirname(fileURLToPath(import.meta.url));
const read = (p) => readFileSync(join(here, '..', p), 'utf8');

test('T7-C1 lib/jsQR.min.js 非空', () => {
  const st = statSync(join(here, '..', 'lib', 'jsQR.min.js'));
  assert.ok(st.size > 5120, 'jsQR.min.js 应大于 5KB');
});
test('T7-C2 receiver.html 关键能力', () => {
  const html = read('receiver.html');
  assert.ok(html.includes("from './core/index.mjs'"));
  assert.ok(html.includes('jsQR.min.js'));
  assert.ok(html.includes('getUserMedia'));
  assert.ok(/indexedDB/i.test(html));
  assert.ok(html.includes('clearDB'));
});
test('T7-C3 receiver.html 拼装/校验/UI', () => {
  const html = read('receiver.html');
  assert.ok(html.includes('reassemble'));
  assert.ok(/缺页|missing/i.test(html));
  assert.ok(/下载|download/i.test(html));
});
test('T7-C4 解析路径与 core 一致（复用 reassemble）', async () => {
  const { buildPages, decodePage, reassemble } = await import('../core/index.mjs');
  const enc = (s) => new TextEncoder().encode(s);
  const text = 'receiver 解析路径一致性测试 中英 mix abc';
  const built = await buildPages(enc(text), 20, 'B', { name:'r.txt', fps:2, ecc:'M' });
  // 模拟 receiver 收到的二维码字符串（第0页 + 数据页）
  const got = [built.metaPage, ...built.dataPages];
  const meta = decodePage(got[0]);
  const metaObj = JSON.parse(meta.payload);
  const collected = new Map();
  for (const s of got.slice(1)) { const d = decodePage(s); collected.set(d.pageNum, d.payload); }
  const res = await reassemble(metaObj, collected);
  assert.equal(res.ok, true);
  assert.equal(res.hashOk, true);
});
```

- [ ] **Step 3: Run test to verify it fails**

Run: `node --test qrpipe/test/receiver-html.test.mjs`
Expected: FAIL（receiver.html 不存在）

- [ ] **Step 4: Write receiver.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>QRPipe 接收端</title>
<!-- jsQR：经典 script，HTTPS 下加载 -->
<script src="lib/jsQR.min.js"></script>
<style>
  body { font-family: system-ui, sans-serif; margin: 0; padding: 12px; }
  #video { width: 100%; max-width: 600px; display: block; }
  canvas { display: none; }
  #info { background:#f5f5f5; padding:10px; border-radius:6px; margin:8px 0; white-space:pre-wrap; font-size:14px; }
  #missing { color:#c00; }
  button { padding:10px 16px; margin:6px 6px 0 0; font-size:16px; }
</style>
</head>
<body>
<h2>QRPipe 接收端</h2>
<video id="video" playsinline autoplay muted></video>
<canvas id="canvas"></canvas>
<div id="info">等待识别任务元信息（第 0 页）…</div>
<div id="missing"></div>
<button id="download" disabled>⬇ 下载还原文件</button>

<script type="module">
import { decodePage, decodeMeta, reassemble } from './core/index.mjs';

const video = document.getElementById('video');
const canvas = document.getElementById('canvas');
const ctx = canvas.getContext('2d', { willReadFrequently: true });
const info = document.getElementById('info');
const missingEl = document.getElementById('missing');
const dlBtn = document.getElementById('download');

let meta = null;                 // 任务元信息（来自第0页）
let collected = new Map();       // pageNum -> payload
let resultBlob = null;
const TASK_KEY = 'qrpipe-task';  // 简化：单任务（按 meta.t）

async function startCamera(){
  try {
    const stream = await navigator.mediaDevices.getUserMedia({ video:{ facingMode:'environment' } });
    video.srcObject = stream;
    requestAnimationFrame(tick);
  } catch(e){ info.textContent = '无法访问摄像头：'+e.message+'\n（需 HTTPS 且授权摄像头）'; }
}

function tick(){
  if(video.readyState === video.HAVE_ENOUGH_DATA){
    canvas.width = video.videoWidth; canvas.height = video.videoHeight;
    ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
    const img = ctx.getImageData(0, 0, canvas.width, canvas.height);
    const code = jsQR(img.data, img.width, img.height);
    if(code) handleDecoded(code.data);
  }
  requestAnimationFrame(tick);
}

function handleDecoded(str){
  const d = decodePage(str);
  if(!d) return;
  if(d.pageNum === 0){
    const m = decodeMeta(d.payload);
    if(!m) return;
    if(meta == null){
      meta = m;
      // 隔日补传：从 IndexedDB 恢复该任务已收页
      loadFromDB().then(()=>{ renderInfo(); tryAssemble(); });
    } else if(meta.t !== m.t){
      // 任务切换：清空旧任务数据，避免污染
      meta = m; collected = new Map();
      clearDB().then(()=> renderInfo());
    } else {
      renderInfo();
    }
    return;
  }
  // 数据页：覆盖式写入（抗偶发误识别，后续正确帧覆盖错误帧）
  if(meta && d.taskId === meta.t){
    if(collected.get(d.pageNum) !== d.payload){
      collected.set(d.pageNum, d.payload);
      savePage(d.pageNum, d.payload);
      renderInfo();
      tryAssemble();
    }
  }
}

function renderInfo(){
  if(!meta){ return; }
  const have = [];
  for(let p=1;p<=meta.pages;p++) if(collected.has(p)) have.push(p);
  const miss = [];
  for(let p=1;p<=meta.pages;p++) if(!collected.has(p)) miss.push(p);
  info.textContent =
    `任务 ${meta.t} · ${meta.name}\n`+
    `大小 ${meta.size} 字节 · 共 ${meta.pages} 页 · ECC ${meta.ecc}\n`+
    `hash ${meta.hash}\n`+
    `已收 ${have.length} / ${meta.pages} 页`;
  missingEl.textContent = miss.length ? ('缺页：'+miss.join(',')) : '✓ 全部页码已收齐，正在校验…';
}

async function tryAssemble(){
  if(!meta) return;
  for(let p=1;p<=meta.pages;p++) if(!collected.has(p)) return; // 未齐
  const res = await reassemble(meta, collected);
  if(res.ok && res.hashOk){
    const blob = new Blob([res.bytes], { type:'text/plain;charset=utf-8' });
    resultBlob = blob;
    dlBtn.disabled = false;
    info.textContent += '\n✓ 校验通过（SHA-256 匹配），可下载。';
    missingEl.textContent = '✓ 完整性校验通过';
  } else if(res.ok){
    missingEl.textContent = '✗ 校验失败：hash 不匹配，请重传可疑页。';
  }
}

dlBtn.addEventListener('click', ()=>{
  if(!resultBlob || !meta) return;
  const a = document.createElement('a');
  a.href = URL.createObjectURL(resultBlob);
  a.download = meta.name || 'restored.txt';
  a.click();
});

// ---- IndexedDB 持久化（隔日补传）----
function openDB(){
  return new Promise((res)=>{
    const r = indexedDB.open(TASK_KEY, 1);
    r.onupgradeneeded = ()=> { r.result.createObjectStore('pages'); r.result.createObjectStore('kv'); };
    r.onsuccess = ()=> res(r.result);
  });
}
async function savePage(num, payload){
  const db = await openDB();
  const tx = db.transaction('pages','readwrite');
  tx.objectStore('pages').put(payload, num);
  return new Promise(r=> tx.oncomplete = r);
}
async function clearDB(){
  const db = await openDB();
  const tx = db.transaction('pages','readwrite');
  tx.objectStore('pages').clear();
  return new Promise(r=> tx.oncomplete = r);
}
async function loadFromDB(){
  const db = await openDB();
  const tx = db.transaction('pages','readonly');
  return new Promise((res)=>{
    const req = tx.objectStore('pages').getAllKeys();
    req.onsuccess = async ()=>{
      const keys = req.result;
      const tx2 = db.transaction('pages','readonly');
      for(const k of keys){ const v = await new Promise(rr=>{ const g=tx2.objectStore('pages').get(k); g.onsuccess=()=>rr(g.result); }); collected.set(k, v); }
      res();
    };
  });
}

startCamera();
</script>
</body>
</html>
```

- [ ] **Step 5: Run test to verify it passes**

Run: `node --test qrpipe/test/receiver-html.test.mjs`
Expected: PASS（4 tests）

- [ ] **Step 6: 手动验证 (T7-C5)**

将 `qrpipe/` 整目录托管到 HTTPS（如 `python3 -m http.server` + 内网穿透，或 GitHub Pages），手机浏览器打开 `receiver.html` → 授权摄像头 → 用 sender 的二维码逐页扫描 → 观察进度/缺页 → 齐全后校验 → 下载还原文件，内容与原文一致。

Expected: 连续识别可用、还原文件内容正确、hash 校验通过。

- [ ] **Step 7: Record diff (不 commit)** — 同 Task 1 Step 5。

---

## Criterion Ownership Matrix

| Criterion ID | Spec 要求 | Owner task | Cross-task deps | Verification (命令/测试 + 预期) | Final-only? |
|---|---|---|---|---|---|
| T1-C1..C5 | 分页确定性/不切断字符/边界/最小步长 (§5) | Task 1 | — | `node --test test/paginate.test.mjs` → PASS | N |
| T2-C1..C7 | 协议编解码 `<T><P>\|<PAYLOAD>` (§4) | Task 2 | — | `node --test test/protocol.test.mjs` → PASS | N |
| T3-C1..C4 | SHA-256 + file:// fallback (§10) | Task 3 | — | `node --test test/hash.test.mjs` → PASS | N |
| T4-C1..C6 | QR 容量/单步上限/时间/版本 (§6) | Task 4 | — | `node --test test/params.test.mjs` → PASS | N |
| T5-C1 (SC3) | 端到端还原 | Task 5 | 1,2,3,4 | `node --test test/e2e.test.mjs` → PASS | N |
| T5-C2 (SC4) | 可复现/隔日补传 | Task 5 | 1,2,3 | 同上 | N |
| T5-C3/C4 (SC5) | 缺页检测/补传 | Task 5 | 1,2,3 | 同上 | N |
| T5-C5 | 默认参数合法 | Task 5 | 1,2,3,4 | 同上 | N |
| T6-C1..C3 | 发送端结构/内联 core/lib | Task 6 | 1–5 | `node --test test/sender-html.test.mjs` → PASS | N |
| T6-C4/C5 (SC6) | 发送端双击可用/重传 | Task 6 | 1–5 | 手动：浏览器双击验证 | Y |
| T7-C1..C4 | 接收端结构/解析路径 | Task 7 | 1–5 | `node --test test/receiver-html.test.mjs` → PASS | N |
| T7-C5 (SC6) | 接收端 HTTPS 连续识别 | Task 7 | 1–6 | 手动：HTTPS 托管 + 手机扫描 | Y |
| SC1–SC5 | 核心逻辑成功标准 | 1–5 | — | `node --test test/` 全绿 | N |
| 数据不经过互联网 (§1.2) | 接收端纯本地处理 | Task 7 | — | 代码审查：无 fetch/XHR 上传，仅 getUserMedia+IndexedDB | Y |
| file:// 双击可用 (§1.2) | 发送端零安装 | Task 6 | — | 手动 + 代码审查（无 CDN/无 module import 本地） | Y |

---

## Self-Review（写 plan 后自查）

1. **Spec coverage**：§1 目标/范围 → 全 task；§2 决策 → 体现于架构；§3 模块 → Task 1–5 + 6/7；§4 协议 → Task 2/5；§5 分页 → Task 1；§6 参数 → Task 4；§7 发送端 → Task 6；§8 接收端 → Task 7；§9 依赖 → Task 6/7 下载 lib；§10 错误处理 → 各 task（空文件/步长/taskId/getUserMedia/hash fallback）；§11 测试 → 各 task TDD；§12 SC → 矩阵覆盖。✅ 无遗漏。
2. **Placeholder scan**：无 TBD/TODO；每步含实际代码与命令。✅
3. **Type consistency**：`paginate`/`encodePage`/`decodePage`/`sha256Hex`/`buildPages`/`reassemble`/`recommendVersion`/`maxStepBytes` 在各 task 签名一致；sender 内联函数名与 core 一致。✅
4. **Acceptance observability**：每条 criterion 为可执行测试或明确手动步骤，含稳定 `T<N>-C<seq>` id。✅

**阶段 3 grilling 修订（源码查证 jsQR + qrcodejs 后，均为代码层修复）**：
- [Critical→已修] qrcodejs 对含非 ASCII（中文）内容注入 UTF-8 BOM(EF BB BF)，jsQR 解码为前导 `\uFEFF`，导致 `decodePage` 把 BOM 当 taskId → 全部中文页返回 null。修复：`core/protocol.mjs decodePage` strip 前导 BOM（T2-C8 测试）。
- [Important→已修] qrcodejs BOM 占 3 字节，sender 容量校验需预留：`recommendVersion(step+3,…)`（Task 6）。
- [Important→已修] sender 步长超 VMAX 未拦截：analyze 时 `ver==null` 则禁用"确认开始"（Task 6）。
- [Important→已修] receiver `!collected.has` 忽略重复帧，偶发误识别无法被后续正确帧覆盖：改为覆盖式 set（Task 7）。
- [Important→已修] IndexedDB 不区分 taskId，任务切换/隔日残留污染：meta.t 变化时清空 collected + `clearDB`（Task 7）。
- [Minor→已修] sender digits 多算 +1（Task 6）；spec V10 容量 Q/H 修正为 151/119。
- [已知限制] qrcodejs 用 `charCodeAt` 编码，emoji（BMP 外代理对）会生成非法 UTF-8 → jsQR 解码失败 → 该页静默丢失。小文本纯文本不含 emoji 时无影响；如需 emoji 支持须换发送端库。
- [源码验证通过] jsQR byte mode 用 `decodeURIComponent` = UTF-8 解码（cozmo/jsQR decodeData/index.ts:136）；qrcodejs 容量表 `QRCodeLimitLength` 与本计划 `QR_CAPACITY` 一致。

**已知 trade-off（诚实记录）**：
- sender.html 内联 core（与 core/*.mjs 重复）是 file:// 双击约束的必要代价；以注释标明、且结构测试保证关键函数在位。
- QR 容量表 V1–10 取自公开参考（thonky/ISO），实现内置查表；锚点测试覆盖 V1.L/V10.M，结构测试覆盖单调性。
- 浏览器层（SC6 手动项）无法纳入 `node --test` verifier，由 final review + 手动验证覆盖。
